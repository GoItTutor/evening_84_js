console.log('Hello from index.js');

const arr = [1, 2, 3];

const [a, b, c] = arr;

console.log(a, b, c);

const isAdmin = true ? true : false;

const arrowFunc = () => {};

const templateString = `${isAdmin}`;
